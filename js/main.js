var colors = ["green", "blue", "red", "yellow"];
var startColors = [];
var simonColors = [];
var userColors = [];

colors.sort(() => Math.random() - 0.5);
startColors = colors[0];
simonColors = startColors;

function startSimon {
  if (simonColors === "blue") {
    
  }
}
